//save name and date about employee
employyes = {};

function addEmployee(num, name, date_on, date_off){
	employyes[num] = [name, date_on, date_off];
}

//finished contract in some times
function getEmployeeByDate(date_empl){
	for(var i = 0; i<emplloyees.count; i++){
		vl = emplloyees
	}
}
